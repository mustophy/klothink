export interface IButton {
    children: React.ReactNode;
    icon?: React.ReactNode;
    className?: string;
}

export interface IProduct {
    name: string;
    image: string;
    price: string;
    desc: string;
}